import { DELAY, APP_COLOR } from './type';

const setObjectName = {
  toast: {
    title: 'title',
    content: 'content',
    type: APP_COLOR.Success,
    delay: DELAY.MIDDLE,
    show: false,
  },
};
